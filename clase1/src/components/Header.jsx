export default function Header() {

  return (
    <header className="contain-header">
      <h1>HOLA COMO VA?</h1>
    </header>
  );
}
// Retornar un solo elemento padre
// Ya no usamos class, usamos className